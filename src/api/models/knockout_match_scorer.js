const knockout_match_scorer = (sequelize, DataTypes) => {
  const Knockout_Match_Scorer = sequelize.define("knockout_match_scorer");
  return Knockout_Match_Scorer;
};

export default knockout_match_scorer;
